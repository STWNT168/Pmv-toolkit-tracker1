# GitHub Repository Setup

Suggested repository name: `spm-tool-kits-reporting`

Upload the project contents directly to the repository root.

`index.html` must remain in the root. No `docs` folder is required.

The included `.github/workflows/pages.yml` deploys the repository root to GitHub Pages.
